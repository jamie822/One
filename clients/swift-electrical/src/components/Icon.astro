---
/* `size` renders as width/height ATTRIBUTES, not CSS.

   Presentation attributes sit below every author stylesheet in the cascade,
   so `.btn svg`, `.card__icon svg`, `.cred__ico svg` and the rest continue to
   win exactly as before and no sized icon moves. What changes is the icons
   nothing sizes at all.

   That was not a hypothetical set. Astro scopes a component's <style> to that
   component's own elements, and the <svg> below belongs to Icon.astro, so it
   never receives the caller's scope hash. Every icon rule written inside a
   caller's <style> block — `.footer li svg`, `.trustbar__item svg`,
   `li svg` in contact.astro — compiled to a selector that matches nothing.
   Those icons had no intrinsic size and no CSS size, so they expanded to fill
   their container: 347x347 in the footer, 360x360 in the contact list.

   The contact list one is a conformance failure, not just a visual bug — a
   360px-wide icon forces 382px of content into a 320px viewport, so the page
   scrolls horizontally. WCAG 1.4.10 Reflow. */
const { name, size = 16 } = Astro.props;
const p = {
  bolt: 'M13 2 3 14h7l-1 8 10-12h-7l1-8z',
  clipboard: 'M9 2h6v3H9zM7 4H5v18h14V4h-2M9 11h6M9 15h4',
  grid: 'M3 3h7v7H3zM14 3h7v7h-7zM3 14h7v7H3zM14 14h7v7h-7z',
  plug: 'M9 2v6M15 2v6M7 8h10v4a5 5 0 0 1-10 0zM12 17v5',
  search: 'M11 4a7 7 0 1 0 0 14 7 7 0 0 0 0-14zM20 20l-4-4',
  phone: 'M22 16.9v3a2 2 0 0 1-2.2 2 19.8 19.8 0 0 1-8.6-3.1 19.5 19.5 0 0 1-6-6A19.8 19.8 0 0 1 2 4.2 2 2 0 0 1 4 2h3a2 2 0 0 1 2 1.7c.1 1 .4 1.9.7 2.8a2 2 0 0 1-.4 2.1L8 10a16 16 0 0 0 6 6l1.4-1.3a2 2 0 0 1 2.1-.5c.9.4 1.8.6 2.8.7a2 2 0 0 1 1.7 2z',
  mail: 'M4 4h16v16H4zM4 6l8 6 8-6',
  clock: 'M12 3a9 9 0 1 0 0 18 9 9 0 0 0 0-18zM12 7v5l3 2',
  pin: 'M12 22s7-6 7-12a7 7 0 1 0-14 0c0 6 7 12 7 12zM12 8a2.5 2.5 0 1 0 0 5 2.5 2.5 0 0 0 0-5z',
  check: 'M20 6 9 17l-5-5',
  arrow: 'M5 12h14M13 6l6 6-6 6',
  shield: 'M12 2 4 5v7c0 5 3.5 8.5 8 10 4.5-1.5 8-5 8-10V5l-8-3zM9 12l2 2 4-4',
  star: 'M12 2l3 6.5 7 .9-5 4.9 1.2 7-6.2-3.3L5.8 21 7 14.3l-5-4.9 7-.9L12 2z',
}[name] || '';
---
<svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7"
     stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
  <path d={p} />
</svg>
